let a = 1;
a = a + 1;  // '+' entre os operandos = infix
a += 1; // binário
a++; // unário  '++'depois do operando = postfix
++a; // unário  '++'antes do operando = prefix
a--; // unário
--a; // unário
console.log(a)